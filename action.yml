name: 'ph-migration-custom-action'
description: 'POC PH Custom Action'
inputs:
  type: 
    description: 'Type of version upgrade'
    required: true
    default: 'patch'
runs:
  using: 'node12'
  main: 'dist/index.js'